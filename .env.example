# Optional: path to SQLite file (default: ./data/leads.db)
# On Railway, set to /app/data/leads.db and mount a volume at /app/data
# DATABASE_PATH=/app/data/leads.db

# Optional: if set, GET /api/leads requires header X-API-Key: <value>
# LEADS_API_KEY=your-secret-key
